import { Reducer, Creators } from './User.Duck';
import Saga from './User.Saga';
import Builder from './User.Builder';

const User = {
    Reducer,
    Creators,
    Saga,
    Builder
};

export default User;
